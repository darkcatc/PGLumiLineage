# 数据库交互
asyncpg>=0.25.0

# SQL 解析
sqlglot>=10.0.0

# LLM 交互
transformers>=4.51.0
openai>=1.0.0

# 配置管理
pydantic>=2.0.0
pydantic-settings>=2.0.0

# Web API (未来可能需要)
fastapi>=0.95.0
uvicorn>=0.20.0

# 任务调度 (未来可能需要)
apscheduler>=3.9.0

# 日志
structlog>=22.3.0

# 工具类
python-dotenv>=0.21.0
toml>=0.10.2

# Apache AGE 图数据库客户端
# age>=0.1.0  # 暂时注释掉，可能需要手动安装或寻找替代方案

# 测试工具
pytest>=7.0.0
pytest-asyncio>=0.20.0

# 开发工具
black>=22.12.0
isort>=5.10.0
flake8>=6.0.0
mypy>=1.0.0
